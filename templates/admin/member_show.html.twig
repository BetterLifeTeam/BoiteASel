{% extends 'base.html.twig' %}

{% block title %}Member{% endblock %}

{% block body %}
    <h1>Member</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ member.id }}</td>
            </tr> #}
            <tr>
                <th>Nom Prénom</th>
                <td>{{ member.firstname }} {{ member.name }}</td>
            </tr>
            {# <tr>
                <th>Firstname</th>
                <td>{{ member.firstname }}</td>
            </tr> #}
            <tr>
                <th>Roles</th>
                <td>{{ member.roles ? member.roles|join(', ') : '' }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ member.email }}</td>
            </tr>
            {# <tr>
                <th>Password</th>
                <td>{{ member.password }}</td>
            </tr> #}
            <tr>
                <th>Money</th>
                <td>{{ member.money }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ member.address }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_members_list') }}">back to list</a>

    <a href="{{ path('admin_member_edit', {'id': member.id}) }}">edit</a>

    {{ include('admin/_delete_form_member.html.twig') }}
{% endblock %}
